models:
  - name: int_sales_analysis
    description: "total invoices, first and last purchase per customer"
    columns: 
      
      - name:  order_id
        tests: 
          - unique
          - not_null
      
      - name: total_invoice
        description: "total order amount, negative values dropped"
        